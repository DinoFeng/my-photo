{
  "name": "nas-photo-manager",
  "version": "1.0.0",
  "description": "NAS Photo Manager - A photo management application for NAS devices",
  "type": "module",
  "scripts": {
    "install:all": "pnpm install && cd backend && pnpm install && cd ../frontend && pnpm install",
    "dev": "concurrently \"cd backend && pnpm dev\" \"cd frontend && pnpm dev\"",
    "dev:backend": "cd backend && pnpm dev",
    "dev:frontend": "cd frontend && pnpm dev",
    "build:backend": "cd backend && pnpm build",
    "build:frontend": "cd frontend && pnpm build",
    "prisma:generate": "cd backend && pnpm prisma generate",
    "prisma:push": "cd backend && pnpm prisma db push"
  },
  "keywords": [
    "photo",
    "manager",
    "nas"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}